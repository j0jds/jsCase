const pessoa = {

}

pessoa.nome = 'FelipeFelipeFelipe';

console.log(pessoa);

// Retorna: {nome: 'FelipeFelipeFelipe'}

/** Isso acontece porque o javascript atribui uma propriedade a um objeto a partir do momento que
essa propriedade é valorizada, independente de o objeto ter ela previamente ou não. */